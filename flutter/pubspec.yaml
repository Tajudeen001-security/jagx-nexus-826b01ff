name: jagx_ai
description: JagX AI 1.1.2 — mobile intelligence console by JagX & JRILICENSE.
publish_to: "none"
version: 1.1.2+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  google_sign_in: ^6.2.2
  flutter_markdown: ^0.7.4+1
  uuid: ^4.5.1
  intl: ^0.19.0
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  share_plus: ^10.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
